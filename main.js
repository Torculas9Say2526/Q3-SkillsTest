function verbNoun() {
    // variable decleration
    const ticket_Price = 250.00;
    const ticket_AdminFee = 50.00;
    let ticket_Quantity = Number(document.getElementById("ticket-quantity").value);
    let subTotal = Number(12.50 * ticket_Quantity);
    let totalPrice = Numver(subTotal + ticket_AdminFee);
    value = 0
    min = 0
}
